# `[standalone]`

Small **standalone** resources that don't belong to a department or the core
FLRP services (HUD tweaks, minor utilities, etc.).

FLRP-authored core logic lives in `[flrp]`; department glue lives in
`[departments]`. Use this folder sparingly.
