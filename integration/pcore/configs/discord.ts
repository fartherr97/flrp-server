// ==========================================================================
// pCore :: configs/discord.ts — FLRP (PROPOSED)
// ==========================================================================
// The bot token and guild id must stay server-side and must never be committed.
// Prefer sourcing them from a private convar / environment at build or runtime
// rather than hardcoding. Placeholders only here.
// ==========================================================================

export const discordConfig = {
    // FLRP Discord bot token — REPLACE_ME (keep secret; do not commit real value)
    botToken: "REPLACE_ME",
    // FLRP guild (server) id — REPLACE_ME
    guildId: "REPLACE_ME"
};
